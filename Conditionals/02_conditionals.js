// Allow the user to access the repositiories if he/she🔥 
// logged from emial
// logged from Google
// logged from apple Id

var email = true;
var goggle = false;
var appleId = false;

if (email) {
    console.log("Logged in successfully😸  with email");
}
if (goggle) {
    console.log("Logged in successfully😸 with goggle");
}
if (appleId) {
    console.log("Logged in successfully😸 with appleId");
}
